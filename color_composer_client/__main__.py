"""Entry point for the Color Composer Client package.

Allows the package to be run with:
    python -m color_composer_client
"""

from color_composer_client.flask_server import main

if __name__ == "__main__":
    main()
